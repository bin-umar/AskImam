#vim:ft=nginx:

server {
	listen 80;
	server_name ask.me;
	location ~*\.\w\w\w?\w?$ { 
		root /home/vagrant/askme/public/;
	}

	location / {
		proxy_set_header Host $http_host;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_pass http://127.0.0.1:8000;
	}
}
